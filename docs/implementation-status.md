# Implementation status

- Current milestone: 2, conversational appearance controls implemented locally; milestone 1 typed chat is deployed and user-reports production chat works. Antigravity is the production provider.
- Actual files/commands: React + TypeScript + Vite frontend in `frontend/`; assistant Markdown rendered with `react-markdown`/GFM and raw HTML skipped; named UI appearance actions shared across manual controls, Antigravity function calls, and imperative WebMCP registration; hex color validation and contrast adjustment; aiohttp API in `backend/`; deterministic Vitest/React Testing Library UI tests; opt-in local Antigravity test with a seven-requests-per-rolling-minute cap; local setup in `README.md`; Netlify and Render deployment configuration.
- Checks passed: `npm test` (8 deterministic UI tests); `npm run test:live` (real local Antigravity color changes and follow-up adjustment, using four model requests); `npm run build`; Python AST syntax validation for `backend/server.py`. User-reported checks: a Hungarian history question returned a correct answer and Google usage appeared under Antigravity / Agents (1/60 RPM, 5.88K/100K TPM, 1/100 RPD); deployed chat smoke and cancellation/recovery checks passed, including follow-up/context behavior after refresh and Markdown styling; an incorrect invitation code returned “That invitation code is not valid.”
- Live checks pending: user smoke test of conversational color changes and follow-up adjustment after deploy; native WebMCP tool invocation still needs a supported browser/agent. The user reports 28 requests in a day completed without quota problems; this confirms that observed usage level, not the provider's full daily limit or sustained quota behavior. Decision: do not implement a Gemini fallback; Antigravity remains the sole production provider.
- Known defects/limits: Bug 1 reproduction: send two related turns, refresh, then ask a context-dependent follow-up. Observed before fix: Antigravity retained the topic because backend interaction IDs were keyed only by the access token, which survives refresh. Expected: the new conversation has no prior topic. Fix: scope Antigravity context to a per-page UUID sent separately from the persistent auth token. User reports the post-refresh follow-up now asks for clarification. Bug 2: assistant Markdown markers such as `**bold**` were shown literally. Fix: render assistant text with `react-markdown` and GFM; raw HTML is skipped. Production build passes; user confirms emphasis and heading markers render as styles. Invitation-attempt and daily chat counters are in-memory and reset on restart; suitable only as local guards, not public quota enforcement. Antigravity quota tier and sustained availability remain account-specific. Conversation state is process-local and resets with backend restarts. Do not add Gemini fallback; the existing manually selectable Gemini adapter is not an automatic failover and is not the production provider. Bugs 3 and 4 (empty/mismatched turns after cancellation) are fixed and covered by deterministic UI tests; the user confirmed production recovery. Canceled questions and partial answers remain copyable and are colored as stale turns, with separate labels. Live Antigravity testing is opt-in, local-backend-only, and capped at seven chat calls per rolling minute. Native WebMCP and all voice milestones are not implemented.
- Latest implementation check: eight deterministic Vitest/RTL tests, one local live Antigravity color-tool test, `npm run build`, and Python AST syntax validation for `backend/server.py` pass. The live test made four model requests and verified the input background changed, then followed a natural-language instruction to adjust that same color.
- Latest deployment observations: uninterrupted sequential questions gave correctly matched answers, including a third contextual question. After the cancellation/order fix, the user reports that stopping before the first token and during streaming no longer produces invalid turns or mismatched subsequent answers. The user also confirmed the orange stale-turn styling for interrupted question and answer, and verified stop-before-first-token recovery. A vague follow-up after a canceled turn asks for clarification; an explicit follow-up about the Thirty Years’ War receives a relevant answer. Render logs did not record the rejected invalid invitation-code attempt; the user confirmed its response was correct. A cold start after Render spun down took about 30 seconds while the UI appeared to think, then returned a proper answer. The user reports 28 requests in a day without quota problems.
- Next concrete step: user manually checks the deployed UI: change the input background to red, ask to make that red lighter/darker, set a text color that needs contrast adjustment, then reset appearance. Confirm the WebMCP notice reflects browser availability. Browser actions must be performed by the user because Browser Use is unavailable in this environment.
