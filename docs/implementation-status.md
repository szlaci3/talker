# Implementation status

- Current milestone: 1, typed chat MVP is deployed and user-reports production chat works. Antigravity is the production provider.
- Actual files/commands: React + TypeScript + Vite frontend in `frontend/`; assistant Markdown rendered with `react-markdown`/GFM and raw HTML skipped; aiohttp API in `backend/`; deterministic Vitest/React Testing Library UI tests; opt-in local Antigravity smoke test with a seven-requests-per-rolling-minute cap; local setup in `README.md`; Netlify and Render deployment configuration. User reports the production deployment is working.
- Checks passed: `npm test` (2 deterministic UI recovery tests); `npm run test:live` (1 real Antigravity response through the rendered UI); `npm run build`; `python -m py_compile backend/server.py`. User-reported checks: a Hungarian history question returned a correct answer and Google usage appeared under Antigravity / Agents (1/60 RPM, 5.88K/100K TPM, 1/100 RPD); deployed chat smoke and cancellation/recovery checks passed, including follow-up/context behavior after refresh and Markdown styling; an incorrect invitation code returned “That invitation code is not valid.”
- Live checks pending: verify the interruption status labels in production; cold-start test after Render has spun down; Gemini fallback and longer-term quota behavior have not been reported as tested.
- Known defects/limits: Bug 1 reproduction: send two related turns, refresh, then ask a context-dependent follow-up. Observed before fix: Antigravity retained the topic because backend interaction IDs were keyed only by the access token, which survives refresh. Expected: the new conversation has no prior topic. Fix: scope Antigravity context to a per-page UUID sent separately from the persistent auth token. User reports the post-refresh follow-up now asks for clarification. Bug 2: assistant Markdown markers such as `**bold**` were shown literally. Fix: render assistant text with `react-markdown` and GFM; raw HTML is skipped. Production build passes; user confirms emphasis and heading markers render as styles. Invitation-attempt and daily chat counters are in-memory and reset on restart; suitable only as local guards, not public quota enforcement. Antigravity quota tier and sustained availability remain account-specific. Conversation state is process-local and resets with backend restarts. Gemini direct remains the fallback. Bugs 3 and 4 (empty/mismatched turns after cancellation) are fixed and covered by deterministic UI tests; the user confirmed production recovery. Canceled questions and partial answers remain copyable and are colored as stale turns, with separate labels. Live Antigravity testing is opt-in, local-backend-only, and capped at seven chat calls per rolling minute. Native WebMCP and all voice milestones are not implemented.
- Latest implementation check: two deterministic Vitest/RTL tests, one local live Antigravity UI smoke test, `npm run build`, and `python -m py_compile backend/server.py` pass. The live test made one model request and returned the expected marker.
- Latest deployment observations: uninterrupted sequential questions gave correctly matched answers, including a third contextual question. After the cancellation/order fix, the user reports that stopping before the first token and during streaming no longer produces invalid turns or mismatched subsequent answers. A vague follow-up after a canceled turn asks for clarification; an explicit follow-up about the Thirty Years’ War receives a relevant answer. Render logs did not record the rejected invalid invitation-code attempt; the user confirmed its response was correct. Cold-start response is pending.
- Next concrete step: verify the new status labels in production, then complete the cold-start test and record its result. Browser actions must be performed by the user because Browser Use is unavailable in this environment.
